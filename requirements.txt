mpmath>=1.3.0
gmpy2>=2.1.0  # strongly recommended: mpmath auto-detects gmpy2 and switches
              # to its compiled GMP backend, giving a benchmarked 28x speedup
              # on PSLQ's big-integer arithmetic vs. pure Python
fpylll>=0.6.0  # required for lll_tests.py (the LLL engine - all headline
cysignals      # results reproduce in <30s); cysignals is its runtime dep
matplotlib>=3.5.0  # optional, for generate_figures.py only
