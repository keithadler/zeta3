# Exact versions this suite was last verified with (macOS arm64, Python 3.14).
# Newer versions will probably work, but a certificate is only reproducible
# if the arithmetic underneath it is; pin first, then upgrade deliberately.
mpmath==1.4.1
gmpy2==2.3.1        # mpmath auto-detects it and switches to the GMP backend
                    # (benchmarked 28x speedup on PSLQ's big-integer arithmetic)
fpylll==0.6.4       # LLL engine (needs the fplll C++ library: brew install fplll
                    # / apt install libfplll-dev / conda install -c conda-forge fpylll)
cysignals==1.12.6   # fpylll runtime dependency
python-flint==0.9.0 # exact Gram-Schmidt certificate in lll_tests.py (FLINT)
cypari2==2.2.4      # pari_mzv_check.py: independent MZV referee (PARI zetamult)
matplotlib>=3.5.0   # optional, generate_figures.py only (not pinned: not part of any certificate)
